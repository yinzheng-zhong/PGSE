include README.md
include LICENSE

# Ship the Rust crate in the sdist so it can be compiled from source.
include native/Cargo.toml
include native/Cargo.lock
recursive-include native/src *.rs

# Never ship build output.
prune native/target
global-exclude *.so *.dylib *.dll *.pyc
