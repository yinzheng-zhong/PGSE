[bdist_wheel]
# The native extension is built against the CPython limited API (abi3, floor 3.10;
# see the pyo3 abi3-py310 feature in native/Cargo.toml). Tag wheels cp310-abi3 so a
# single wheel per platform serves CPython 3.10+.
py_limited_api = cp310
